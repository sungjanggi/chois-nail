/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // If you later want to load external images, add domains here.
    // domains: ["images.unsplash.com"],
  },
};

export default nextConfig;
